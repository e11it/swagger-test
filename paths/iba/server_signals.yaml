get:
  tags:
    - "iba"
  summary: "Return IBA signal mapping"
  description: ""
  operationId: "getClients"
  produces:
  - "application/json"
  parameters:
    - in: query
      name: limit
      type: integer
      default: 100
      minimum: 1
      maximum: 1000
    - in: query
      name: offset
      type: integer
      default: 0
  responses:
    200:
      description: "OK"
      schema:
        type: object
        properties:
          total:
            type: integer
            example: 100
          items:
            type: array
            items:
              $ref: "definitions/clients.yml#/ClientBaseObject"